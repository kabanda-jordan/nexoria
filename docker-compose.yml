services:
  nexora:
    build:
      context: .
      dockerfile: Dockerfile
    image: nexora-rwanda-marketplace
    container_name: nexora
    ports:
      - "8080:3001"
    env_file:
      - .env
    restart: unless-stopped
